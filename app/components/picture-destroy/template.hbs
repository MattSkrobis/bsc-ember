{{#if inForm}}
    {{#paper-button onClick=(action 'remove' picture) 
      iconButton=true}}
    {{paper-icon "delete forever" size=36}}
      {{#paper-tooltip}}
        Delete
      {{/paper-tooltip}}
  {{/paper-button}}
{{/if}}