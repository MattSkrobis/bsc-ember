import Ember from 'ember';

const { Component, inject: { service }, computed } = Ember;

export default Component.extend({
  // shoppingCart: service(),
  // session: service(),
  // init() {
  //   this.get('shoppingCart.getCartOrder')();
  // },
  // actions: {
  //   addOrderLine() {
  //     this.get('shoppingCart.addOrderLine');
  //   }
  // }
});
