<h1>Logowanie</h1>

<form {{action 'authenticate' on='submit'}}>
  <label for="identification">E-mail</label>
  {{input id='identification' placeholder="E-mail" value=identification}}
  <label for="password">Hasło</label>
  {{input id='password' placeholder="Hasło" type='password' value=password}}
  <button type="submit">{{t 'default.login'}}</button>
  {{#if errorMessage}}
    <p>{{errorMessage}}</p>
  {{/if}}
</form>

{{#paper-button}}
  {{#link-to "password-forgotten"}} Nie pamiętasz hasła? {{/link-to}}
{{/paper-button}}